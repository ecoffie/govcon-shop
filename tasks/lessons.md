# Lessons

- 2026-05-11: For GovCon Shop, treat `shop.govcongiants.com` as the primary production domain. `shop.govcongiants.org` should redirect to `.com`; if `.com` fails, check DNS/domain configuration first.
